output ocids {
    value = local.ocids
}

output locations {
    value = local.locations
}

output tag_sets {
    value = local.tag_sets
}

output tag_set_default {
    value = local.tag_set_default
}